<div class="destinations">

	<h3><%= destination.name %></h3>
	<img src="<%= destination.image_url %>" />
</div>

<div class="todo_items">
	<% destination.todo_items.each do |buttplug| %>
	<%= render buttplug %>
	<% end %>
</div>
